# wrangler and @cloudflare/next-on-pages disagree on an optional peer
# (@cloudflare/workers-types v5 vs v4). Neither actually needs it here, but npm
# treats the clash as fatal. Set in the repo so Cloudflare's own install
# behaves exactly like a local one.
legacy-peer-deps=true
