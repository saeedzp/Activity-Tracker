# Activity Tracker — قواعد المشروع الثابتة

هذه القواعد لا تتغيّر. أي كود يخالفها يُعتبر خطأ.

## اللغة

- **التخزين والتصدير بالإنجليزي دائماً**: الحالات، أكواد الأسباب، أنواع الاستاند،
  وكل ما يذهب إلى قاعدة البيانات أو ملف التصدير.
- **العرض بالعربي**: الترجمات في `src/lib/domain.ts` (`STATUS_AR`, `REASON_AR`)
  وهي للعرض فقط. لا تخزّن نصاً عربياً في أي عمود.
- الواجهة RTL بالكامل، mobile-first.
- **لا تذكر في الواجهة كلمة «مرچندايزر» أو «تيم ليدر» إطلاقاً.** شاشة الدخول
  فيها حقل الرقم الوظيفي فقط.

## الحالات وأكواد الأسباب — ثابتة، لا تُخترع غيرها

```
Implemented                  → Low Stock | POSM not received | Without POSM        [يقفل]
Implemented in another store → بدون كود سبب، مع اسم السوق الفعلي (alt_store_name)   [يقفل]
Not Implemented              → Account Restriction | Contract Issue | OOS | Space Issue
                               | POSM not received | Stand not received | Stand Damaged
                               | Stand Missing | Store Refused | Store renovation
                               | Store Temporarily Closed | Store Permanently Closed
                               | Other                                             [يبقى مفتوح]
```

مصدر الحقيقة الوحيد: `src/lib/domain.ts`، ومطبَّق أيضاً كـ CHECK constraint في
`supabase/migrations/0001_init.sql`.

## القاعدة الحرجة — الإقفال

السطر يقفل **فقط** عند `Implemented` أو `Implemented in another store`.
أي حالة أخرى تبقى مفتوحة ويرجع الاكتفيتي لقائمة الموظف.
استعمل `isClosing()` — لا تكرر الشرط يدوياً في أي مكان.

## أنواع الاكتفيتي

التخزين والتصدير: `50X50 · 1x1 · 2x1 · 2x2 · 3x2 · 6x2 · GE · GMU · Rebrandable`

العرض بالعربي (`DISPLAY_TYPE_AR`): `GE` = القندولة ·
`Rebrandable` = استاند حديد قابل لتغيير المواد الدعائية · والباقي كما هو.

الواجهة تسمّي الحقل **«نوع الاكتفيتي»** لا «نوع الاستاند».

## الاكتفيتي = حملة، لا مقاس

ثلاثة أشياء منفصلة، لا تخلطها:

| | مثال | العمود |
|---|---|---|
| **اسم الاكتفيتي** | العودة للمدارس | `activities.name` |
| **البراندات** | تويكس · سنيكرز — **أكثر من واحد** | `activities.brands text[]` |
| **المقاس** | 2×2 · القندولة | `submissions.display_type` |

البراند خاصية للحملة لا قائمة مستقلة، ولذلك **لا توجد شاشة براندات**.

`/admin/activities`: تختار الشهر وتضيف الحملات — اسم وبراندات. **المقاس لا
يُخطَّط**: الموظف يبلّغ بالمقاس اللي وصل فعلاً، وهو ليس دائماً المقصود.

شاشة الموظف: **يختار الاكتفيتي ثم المقاس**، ثم سؤال الدخول.

الإدخال **ينسخ** اسم الحملة وبرانداتها وشهرها (`submissions.activity_name`
و`brands` و`month`) ولا يكتفي بالإشارة إليها — تعديل حملة بعد شهور يجب ألا
يعيد كتابة ما بلّغ عنه الموظف وقتها.

## سؤال الإدخال — واحد لا اثنان

سؤال واحد: **«هل تم إدخال الاستاند للسوق؟»** بثلاث إجابات تقابل الحالات الثلاث:

| الإجابة | الحالة | ما يظهر |
|---|---|---|
| نعم، دخل السوق | `Implemented` | صورة واحدة **إجبارية** + صور إضافية اختيارية |
| لا، ما دخل | `Not Implemented` | الأسباب؛ و`Other` تفتح خانة نص إجبارية · بدون صور |
| دخل سوق آخر | `Implemented in another store` | اسم السوق الفعلي |

`Not Implemented` يبقى مفتوحاً — الموظف يرجع يسجّل لما يدخل الاستاند.

## سؤال المواد الدعائية المخصصة

`GE` و`GMU` و`Rebrandable` تجهيزات دائمة تبقى في السوق ويُعاد تلبيسها كل حملة،
فالسؤال الوحيد المفيد عنها: **«هل تم تركيب مواد دعائية مخصصة؟»** نعم/لا.

يظهر فقط عند اختيار أحد الثلاثة **و** الإجابة «نعم، دخل السوق».
يُخزَّن في `submissions.custom_posm`، و`null` في كل حالة أخرى — لأن `false`
تعني «ما رُكّبت» بينما `null` تعني «السؤال ما طُرح».
مصدر الحقيقة: `CUSTOM_POSM_TYPES` و`asksCustomPosm()`.

## كود السبب — متى يُطلب

- `Not Implemented`: **إجباري**، و`Other` تتطلب نصاً في `note`.
- `Implemented`: **اختياري** — الأكواد الثلاثة تصف حالة تم التطبيق رغمها.
- `Implemented in another store`: بدون كود، مع `alt_store_name`.

## التواريخ

التخزين ISO. العرض بالعربي عبر `formatDateAr` بأسماء الشهور المستعملة في
السعودية (سبتمبر، أغسطس) لا الشامية (أيلول، آب).

## الصور

ضغط في المتصفح قبل الرفع: أقصى ضلع `1600px` وجودة `0.85` (`src/lib/photo.ts`).
`createImageBitmap` يطبّق دوران EXIF، وإلا انحفظت الصور مقلوبة.

## submissions سجل أحداث

`submissions` **append-only**: كل تحديث سطر جديد، ولا نعدّل القديم أبداً.
آخر سطر لكل `(store_id, brand, display_type)` هو الحالة الحالية (`current_state` view).

## الوقت

`submitted_at` من السيرفر دائماً — لا تثق بساعة الجوال، ولا تعرض القيمة للموظف.

## المطابقة

`src/lib/match.ts` — أربع مراحل بالترتيب الصارم:
`store_aliases` ← رقم السوق + الأكاونت (بشرط ألا يكون صفراً أو فاضياً) ←
اسم تقريبي داخل نفس الأكاونت بعتبة `0.62` ← `unlinked`.
أي نتيجة أقل من `0.80` تُعلَّم «مطابقة ضعيفة» للمراجعة.

توحيد إجباري قبل أي مقارنة:
`PANDA/Panda/panda → panda` · `BD/Bindawood/Bin Dawood → bin dawood` ·
`Tier 3 → other mt` · قص المسافات الزائدة · `Madinah → West`.

## الروت — رفع من الواجهة

`/admin/route` يستقبل ملف `.xlsx` أو `.csv` بنفس أعمدة ملف الأسواق.

**الملف هو الروت** — استبدال كامل بخطوة واحدة، بلا شاشة موافقة. كل سوق في
الملف يُحفظ، وأي سوق غير موجود فيه **يُعطَّل تلقائياً**.

- الربط بـ `STORE ID`: تغيير اسم السوق **يُحدَّث** ولا يُنشئ سوقاً جديداً.
- الملخص بعد الرفع إيصال لا بوابة، لكنه **يسمّي الأسواق المعطَّلة** — بها
  يُكتشف الملف الناقص فوراً بدل ما يكتشفه موظف بعد أيام.
- المنطق في `src/lib/route-import.ts` — كود صافي بلا قاعدة بيانات، مختبر.
- القارئ في `src/lib/xlsx.ts` مشترك بين السكربت والمسار — قارئ واحد لا اثنان.
- سوق غير موجود في الملف **يُعطَّل ولا يُحذف** (`stores.active`)، لأن إدخالاته
  السابقة تبقى والتصدير يحتاجه. التعطيل بموافقة صريحة، مو تلقائياً.
- قوائم الموظفين تعرض `active = true` فقط.

الموظف **لا يُطالب بإدخال كل أسواقه** — يختار السوق الذي وصله استاند ويسجّله،
والباقي يُترك بلا إدخال.

## بيانات الأسواق

ملف الأسواق الأصلي `.xlsx` والـ CSV المولّد منه **لا يُرفعان أبداً** (فيهما أسماء وأرقام
موظفين). المرفوع: `scripts/xlsx-to-csv.ts` و `seed/stores.example.csv`.

حقائق ثابتة عن المصدر، لا تفترض غيرها:
- الملف فيه صفوف مكررة لنفس `STORE ID` — المحوّل يوحّدها ويطبع أي تعارض بدل ما يخمّن.
- `Retailer NO.` غير موثوق: صفر أو نص في ربع الأسواق تقريباً.
  **المطابقة تعتمد على `mars_code` أولاً**، و`retailer_no` احتياطي فقط.
- أي رقم سوق غير رقمي أو يساوي صفراً يُرفض (`isUsableStoreNo`) ولا يُخزَّن.
- رقم واحد يشير لسوقين = بيانات خاطئة، ما هو مطابقة: يروح لشاشة الربط.

## الشهر والسجل

الاكتفيتي يمشي بالشهور. العمود اسمه `month` بصيغة `YYYY-MM` في `activities`
و`submissions` — **لا تسمّه `period`**.

`submissions.month` **لا يُؤخذ من تاريخ الإرسال**. موظف يسجّل في ٣ أكتوبر
استاند حملة سبتمبر يقدّم تقريراً عن سبتمبر، وحسابه على أكتوبر يفسد كل مقارنة
شهرية.

الشهر يُؤخذ من **الحملة نفسها** (`activities.month`)، لا من تاريخ الإرسال.

`/admin/history` يعرض كل إدخال بكامل حقوله مع تصدير CSV (بـ BOM وإلا إكسل
يقرأ العربي مشوّهاً). يقرأ من كل الأسواق بما فيها المعطّلة — السجل لازم يحلّ
سوقاً أُغلق منذ ذلك الحين.

## التصدير

نفس أعمدة مارس الـ20 بالترتيب. عند `Implemented in another store` يُكتب اسم السوق
الفعلي في `Additional Comments`.

## النشر — Cloudflare Pages دائماً

**كل مشاريع هذا المستخدم تُنشر على Cloudflare Pages، لا Workers.** لا تقترح
Workers ولا تحوّل المشروع إليه.

يترتب على هذا قيود لازمة:

- المحوّل هو `@cloudflare/next-on-pages`، ومخرجاته `.vercel/output/static`.
- **كل** route و page لازم فيها `export const runtime = "edge"`. أي مسار بدونها
  يفشل البناء.
- ممنوع استعمال `node:` APIs في كود يعمل على السيرفر. استعمل Web APIs:
  `crypto.subtle` بدل `node:crypto`، و`atob/btoa` بدل `Buffer`.
- `next` مثبّت على `15.5.2` **بالضبط بدون caret** — `next-on-pages` لا يدعم
  أحدث منها، وأي caret يجعل npm عند Cloudflare يتجاوزها فيفشل البناء.
- `.npmrc` فيه `legacy-peer-deps=true` لتعارض تبعية اختيارية بين `wrangler`
  و`next-on-pages`. لا تحذفه.
- `nodejs_compat` مطلوب كـ compatibility flag في إعدادات Pages.
- **ممنوع وجود `wrangler.jsonc` أو `wrangler.toml` في الريبو.** وجوده يجعل
  Cloudflare يتجاهل متغيرات البيئة المضبوطة في اللوحة. اللوحة هي المرجع
  الوحيد، والرايات تُمرَّر صراحةً في سكربتات npm المحلية.

إعدادات لوحة Cloudflare:

| الحقل | القيمة |
|---|---|
| Build command | `npm run cf:build` |
| Build output directory | `.vercel/output/static` |

## الأمان والـ Git

- لا تكتب أي مفتاح حقيقي في أي ملف داخل الريبو. المفاتيح في `.env.local` فقط.
- لا ترفع ملفات فيها أسماء موظفين أو أرقامهم. `seed/stores.csv` يبقى محلياً؛
  المرفوع هو `seed/stores.example.csv`.
- قبل أي push: `npm run build` و `npm test` بدون أخطاء.
- رسائل الـ commit بصيغة Conventional Commits.
